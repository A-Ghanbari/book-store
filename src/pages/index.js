import { Col, Row } from "antd";
import Head from "next/head";
import Link from "next/link";
import HomeCategory from "../components/Home/HomeCategory";
import HomeLogin from "../components/Home/HomeLogin";
import HomeSwiper from "../components/Home/HomeSwiper";
import LastBook from "../components/Home/LastBook";
import TopSlider from "../components/Home/TopSlider";
import { fetchEntries } from "../helper/contentfull";

export default function Home({ books }) {
  return (
    <div>
      <Head>
        <title>کتاب فروشی |صفحه اصلی</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <TopSlider />
      <HomeSwiper books={books} />
      <HomeLogin />
      <HomeCategory />
      <LastBook book={books[0]} />
    </div>
  );
}

export async function getServerSideProps() {
  const { items: books } = await fetchEntries("book");
  return {
    props: { books },
  };
}
